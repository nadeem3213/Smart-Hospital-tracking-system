# Frontend environment variables (Vite requires VITE_ prefix)

# Backend API base URL
VITE_API_BASE=http://localhost:5000

# Default map center coordinates (used as fallback when geolocation is denied)
VITE_DEFAULT_LAT=18.44271143872656
VITE_DEFAULT_LNG=73.83139194092284